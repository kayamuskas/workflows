name: "Req get and print"

on:
  workflow_call:
    inputs:
      req:
        description: Get reguest
        required: false
        type: string
        default: sample
        
jobs:
  print_req:
    runs-on: ubuntu-latest
    steps:
    - name: "Get req and print"
      id: req
      run: |
        echo ${{ inputs.req }}
    - name: "Step 2"
      run: |
        echo step reused 2
        curl https://kayama.me/${{ secrets.SOME_SECRET }}
        echo '----------------------------------------------------------------------------------------------------'
        echo '${{ vars.ENV_OUTSIDE }}'
        echo '----------------------------------------------------------------------------------------------------'
        echo '${{ vars.TEST_ENV_VAR }}'
        echo '----------------------------------------------------------------------------------------------------'
        echo '${{ env.TEST_JOB_ENV }}'
        echo '----------------------------------------------------------------------------------------------------'
